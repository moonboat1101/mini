# Moonboat Mini UI 约定

这份文件记录项目中反复确认过的 UI 规则。修改页面前先阅读，并优先复用现有主题变量和组件样式。

## 主题与色彩

- 全局主题在 `src/styles/theme.less`：纯白页面底色、浅灰卡片、黑灰文字和中性灰结构。整体不使用暖色装饰，不使用阴影建立层次。
- 页面背景使用 `var(--color-bg-page)`；大卡片使用 `var(--color-bg-card)`；需要更明显层次时使用 `var(--color-bg-card-soft)` 或 `var(--color-bg-elevated)`。
- 中性色统一使用黑、白、灰，不要引入大面积暖黄或随页面功能随意分配主题色；服务器、状态、警告等必要的小型语义标签可以保留明确的识别色。功能差异通过内容、图标和插画表达。
- 页面层级统一为“纯白页面 → 浅灰一级容器 → 白色或更浅灰二级内容”；不要在白底上直接堆叠多种彩色面板。

## 输入框

- 所有 `Input`、`Textarea` 和批量编辑输入区域统一使用比二级白色内容更深的 `#e8e8e8` 浅灰底色、无边框、无阴影，通过色差区分输入区域。
- 输入框相邻的提交、确认和查询按钮使用 `var(--surface-primary)` 近黑底色与白色文字；不要恢复暖色按钮。
- 输入时使用 `src/hooks/useKeyboardFloating.ts` 的悬浮适配：按实际遮挡距离上移输入区，并关闭系统的重复自动避让；不要新增额外的页面高度缩短或第二套位移逻辑。

## 字号

- 页面开发和样式调整优先使用 `src/styles/theme.less` 中的四档字号变量：`@font-size-secondary`（20px）、`@font-size-body`（24px）、`@font-size-title`（30px）、`@font-size-page-title`（36px）。
- 等于这四档的字号禁止直接硬编码 `20px`、`24px`、`30px`、`36px`，必须使用对应变量；已有变量定义中的基础值除外。
- 所有新增或修改的字号必须是偶数，严禁使用任何奇数字号。

## 代码清理

- 某个功能、结构或样式后续不再使用时，必须同步删除相关的 JSX/TS 逻辑、样式类、变量和辅助代码，不保留无引用的遗留代码。

## 卡片与容器

- 默认无边框。优先用留白、背景色差和圆角建立层次。
- 常规卡片使用 `var(--color-bg-card)`，不使用外阴影、内描边或左上角装饰高光。
- 页面大容器和列表项避免使用明显渐变。页面底色统一为纯白；常规卡片使用 `var(--color-bg-card)`，需要次级层次时使用 `var(--color-bg-card-soft)`，不使用阴影。
- 不要新增渐变；页面保持简约的黑白灰关系。
- 卡片的外部间距和内部 padding 要分开处理。需要增加与外框的距离时改 `margin` 或父容器间距，不要误加到内容的 `padding`。

## 按钮

- 普通功能按钮统一使用 16px 圆角、无边框、无伪元素边框，并保持内容自适应高度。不要为了填充空间把按钮撑高。
- 主按钮使用 `var(--surface-primary)`（近黑）和 `var(--color-primary-content)`（白色）；普通按钮文字使用默认字重 `400`，除非它是标题、标签或明确需要强调的状态。
- `@radius-pill` 只用于明确的胶囊标签、筛选 chip 等小型控件；普通操作按钮不要使用胶囊圆角。
- Taro `Button` 的 `::after` 边框要清除，避免出现系统默认描边。
- 按钮按下状态不能让背景突然消失。需要反馈时优先使用轻微位移或透明度变化，并保留原有底色。
- 底部导航保持半透明白色背景和独立的中灰选中态，不使用阴影；导航高亮不要复用主按钮的处理方式。

## 间距与对齐

- 同一组控件使用一致的高度、圆角和间距。市场筛选按钮当前使用 58px 高、16px 圆角，页面内其他普通按钮按同一视觉规范处理。
- 图标、文字、箭头需要在同一行时使用 flex 对齐；内容文字靠近左侧图像，右侧只保留箭头或操作符号。
- 元素条等横向内容的右侧外间距应与容器顶部间距一致。增加外部距离时使用 `margin-right`，不要把它混入内部 padding。
- 文字和数据需要水平居中时，对标签和数值分别设置 `text-align: center`，不要依赖不稳定的 padding 偏移。
