# 圣牌市场云开发配置

本项目只使用一个云数据库集合：`users`。每条记录为一名微信用户的圣牌资料；`isPublished: true` 的记录会直接出现在市场列表。

在微信开发者工具中打开项目后：

1. 点击工具栏的“云开发”，按引导开通免费环境，并将其设为当前小程序环境。
2. 在“数据库”新建集合 `users`。
3. 将 `cloudfunctions/cardExchangeUser` 上传部署为云函数（右键该目录，选择“上传并部署：云端安装依赖”）。它负责按微信 OpenID 读取和保存当前用户，保证一人一条资料。
4. 给集合设置下列权限规则，保证已发布资料可被读取：

```json
{
  "read": "doc.isPublished == true",
  "write": false
}
```

云函数使用服务端身份写入，并以 `_openid` 查询后更新或新增。应用启动时调用 `wx.cloud.init()`，无需把环境 ID 写死在代码中；绑定当前环境即可。
